{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test_coverage",
            "type": "shell",
            "command": "source env/bin/activate && pytest --cov='Python_Testing' --cov-report html tests/test_*.py"
        }
    ]
}